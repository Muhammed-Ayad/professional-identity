class: SocialLink
table: social_link
fields:
  profile: Profile?, relation(onDelete=Cascade)
  platform: String
  url: String
  label: String?
  sortOrder: int, default=0
  createdAt: DateTime, default=now
  updatedAt: DateTime, default=now

indexes:
  social_link_profile_id_idx:
    fields: profileId
