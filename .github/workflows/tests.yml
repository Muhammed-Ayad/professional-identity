name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '4.0.0'
      SERVERPOD_PASSWORD_database: 'FZPSTkci3lZhzsUSfgNUXkFHtnnjvsCQ'
      SERVERPOD_PASSWORD_redis: 'jk817xxpgYU0ueh7peRQLz_VpiYC5fIN'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.4'

      - name: Start containers
        working-directory: professional_identity_server
        run: docker compose up --quiet-pull --build -d

      - name: Install dependencies
        run: flutter pub get

      - name: Install Serverpod CLI
        run: dart install serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: professional_identity_server
        run: serverpod generate

      - name: Run tests
        working-directory: professional_identity_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: professional_identity_server
        run: docker compose down -v
